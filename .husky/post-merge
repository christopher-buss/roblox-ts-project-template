# matches only the package-lock.json inside project directory
npx git-pull-run --pattern "pnpm-lock.yaml" --command "ni"

# check for mise.toml changes and run mise install if updated
npx git-pull-run --pattern "mise.toml" --command "mise install"